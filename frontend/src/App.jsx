import React,{useState} from 'react'; import Login from './pages/Login'; import Dashboard from './pages/Dashboard'; export default function App(){ const [token,setToken]=useState(localStorage.getItem('sf_token')); if(token){ return <Dashboard token={token} onLogout={()=>{setToken(null);localStorage.removeItem('sf_token')}}/> } return <Login onLogin={(t)=>{setToken(t);localStorage.setItem('sf_token',t)}}/> }